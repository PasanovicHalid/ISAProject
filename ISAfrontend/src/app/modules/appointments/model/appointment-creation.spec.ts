import { AppointmentCreation } from './appointment-creation';

describe('AppointmentCreation', () => {
  it('should create an instance', () => {
    expect(new AppointmentCreation()).toBeTruthy();
  });
});
